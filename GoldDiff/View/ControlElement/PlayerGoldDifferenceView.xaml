<UserControl x:Class="GoldDiff.View.ControlElement.PlayerGoldDifferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GoldDiff.View.ControlElement"
             xmlns:view="clr-namespace:GoldDiff.Shared.View;assembly=GoldDiff.Shared"
             xmlns:converter="clr-namespace:GoldDiff.View.Converter"
             xmlns:game="clr-namespace:GoldDiff.LeagueOfLegends.Game"
             xmlns:settings="clr-namespace:GoldDiff.View.Settings"
             xmlns:converter1="clr-namespace:FlatXaml.Converter;assembly=FlatXaml"
             xmlns:sharedConverter="clr-namespace:GoldDiff.Shared.View.SharedConverter;assembly=GoldDiff.Shared"
             xmlns:theme="clr-namespace:FlatXaml.Theme;assembly=FlatXaml"
             xmlns:flat="clr-namespace:FlatXaml.View;assembly=FlatXaml"
             xmlns:sharedIcon="clr-namespace:GoldDiff.Shared.View.SharedIcon;assembly=GoldDiff.Shared"
             mc:Ignorable="d"

             x:Name="This"

             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <converter:IntToLoLGoldValueConverter x:Key="IntToLoLGoldValueConverter" />
        <sharedConverter:GreaterThanZeroToVisibilityConverter x:Key="GreaterThanZeroToVisibilityConverter"
                                                              TrueValue="Visible"
                                                              FalseValue="Collapsed" />
        <converter1:BoolToVisibilityConverter x:Key="TrueToVisibleConverter"
                                              TrueValue="Visible"
                                              FalseValue="Collapsed" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" /> <!-- Role Icon Blue -->
            <ColumnDefinition Width="*" MinWidth="32" MaxWidth="96" /> <!-- Champion Icon Blue -->
            <ColumnDefinition Width="1*" MinWidth="32" /> <!-- Stats Blue -->
            <ColumnDefinition Width="Auto" /> <!-- Gold Difference -->
            <ColumnDefinition Width="1*" MinWidth="32" /> <!-- Stats Red -->
            <ColumnDefinition Width="*" MinWidth="32" MaxWidth="96" /> <!-- Champion Icon Red -->
            <ColumnDefinition Width="Auto" /> <!-- Role Icon Red -->
        </Grid.ColumnDefinitions>

        <!-- Role Icon Blue Side -->

        <local:LoLPositionView Grid.Column="0"
                               Margin="0, 0, 12, 0"
                               MaxWidth="{DynamicResource {x:Static theme:FlatThemeKeys.FontSizeRegular}}"
                               MaxHeight="{DynamicResource {x:Static theme:FlatThemeKeys.FontSizeRegular}}"
                               Position="{Binding ElementName=This, Path=Position}" />

        <!-- /Role Icon Blue Side -->

        <!-- Champion Tile Blue Side -->

        <Image Grid.Column="1"
               Margin="0, 0, 12, 0"
               VerticalAlignment="Center"
               AllowDrop="{Binding ElementName=This, Path=CanSwapPlayers}"
               DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
               Source="{Binding Path=Champion.SmallTileImage}"
               MouseLeftButtonDown="ChampionTileBlueSide_OnMouseLeftButtonDown"
               DragEnter="ChampionTileBlueSide_OnDragOver"
               DragOver="ChampionTileBlueSide_OnDragOver"
               Drop="ChampionTileBlueSide_OnDrop" />

        <!-- /Champion Tile Blue Side -->

        <!-- Stats Blue Side -->

        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Gold -->

            <flat:FlatIcon Grid.Column="0"
                           Grid.Row="1"
                           Margin="0, 0, 6, 0"
                           Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.Coins}}" />

            <TextBlock Grid.Column="1"
                       Grid.Row="1"
                       Margin="0, 0, 6, 0"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       DataContext="{Binding ElementName=This, Path=GoldComparisonHelper, Mode=OneWay}"
                       Text="{Binding Path=GoldBlueSide, Mode=OneWay, Converter={StaticResource IntToLoLGoldValueConverter}}" />

            <!-- /Gold -->

            <!-- KDA -->

            <StackPanel Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Grid.Row="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        DataContext="{x:Static settings:ViewSettings.Instance}"
                        Visibility="{Binding Path=DisplayPlayerStats, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource TrueToVisibleConverter}}">

                <!-- Kills -->

                <flat:FlatIcon Margin="0, 0, 6, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLKills}}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Text="{Binding Path=Kills, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.PositiveRegular}}"
                           Visibility="{Binding Path=KillsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=KillsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <!-- /Kills -->

                <TextBlock Margin="0, 0, 6, 0"
                           Text="/" />

                <!-- Deaths -->

                <flat:FlatIcon Margin="0, 0, 6, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLDeaths}}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Text="{Binding Path=Deaths, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.NegativeRegular}}"
                           Visibility="{Binding Path=DeathsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=DeathsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <!-- /Deaths -->

                <TextBlock Margin="0, 0, 6, 0"
                           Text="/" />

                <!-- Assists -->

                <flat:FlatIcon Margin="0, 0, 6, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLAssists}}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Text="{Binding Path=Assists, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="0, 0, 6, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerBlueSide}"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.PositiveRegular}}"
                           Visibility="{Binding Path=AssistsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=AssistsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <!-- /Assists -->

            </StackPanel>

        </Grid>

        <!-- /Stats Blue Side -->

        <!-- Gold Difference -->

        <local:GoldDifferenceView Grid.Column="3"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  MinorGoldDifference="{x:Static game:LoLConstants.MinorPlayerGoldDifference}"
                                  MediocreGoldDifference="{x:Static game:LoLConstants.MediocrePlayerGoldDifference}"
                                  LargeGoldDifference="{x:Static game:LoLConstants.LargePlayerGoldDifference}"
                                  GoldOwnerBlueSide="{Binding ElementName=This, Path=PlayerBlueSide, Mode=OneWay}"
                                  GoldOwnerRedSide="{Binding ElementName=This, Path=PlayerRedSide, Mode=OneWay}" />

        <!-- /Gold Difference -->

        <!-- Stats Red Side -->

        <Grid Grid.Column="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Gold -->

            <TextBlock Grid.Column="0"
                       Grid.Row="1"
                       Margin="6, 0, 0, 0"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       DataContext="{Binding ElementName=This, Path=GoldComparisonHelper, Mode=OneWay}"
                       Text="{Binding Path=GoldRedSide, Mode=OneWay, Converter={StaticResource IntToLoLGoldValueConverter}}" />

            <flat:FlatIcon Grid.Column="1"
                           Grid.Row="1"
                           Margin="6, 0, 0, 0"
                           Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.Coins}}" />

            <!-- /Gold -->

            <!-- KDA -->

            <StackPanel Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Grid.Row="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        DataContext="{x:Static settings:ViewSettings.Instance}"
                        Visibility="{Binding Path=DisplayPlayerStats, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource TrueToVisibleConverter}}">

                <!-- Kills -->

                <TextBlock Margin="6, 0, 0, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Text="{Binding Path=Kills, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="6, 0, 0, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.PositiveRegular}}"
                           Visibility="{Binding Path=KillsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=KillsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <flat:FlatIcon Margin="6, 0, 0, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLKills}}" />


                <!-- /Kills -->

                <TextBlock Margin="6, 0, 0, 0"
                           Text="/" />

                <!-- Deaths -->

                <TextBlock Margin="6, 0, 0, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Text="{Binding Path=Deaths, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="6, 0, 0, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.NegativeRegular}}"
                           Visibility="{Binding Path=DeathsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=DeathsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <flat:FlatIcon Margin="6, 0, 0, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLDeaths}}" />

                <!-- /Deaths -->

                <TextBlock Margin="6, 0, 0, 0"
                           Text="/" />

                <!-- Assists -->

                <TextBlock Margin="6, 0, 0, 0"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Text="{Binding Path=Assists, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Margin="6, 0, 0, 0"
                           Foreground="{DynamicResource {x:Static theme:FlatThemeKeys.PositiveRegular}}"
                           DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
                           Visibility="{Binding Path=AssistsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                    <Run Text="(+" />
                    <Run Text="{Binding Path=AssistsSinceLastItemAcquisition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Run Text=")" />
                </TextBlock>

                <flat:FlatIcon Margin="6, 0, 0, 0"
                               Source="{DynamicResource {x:Static sharedIcon:GoldDiffSharedIconKeys.LoLAssists}}" />


                <!-- /Assists -->

            </StackPanel>

        </Grid>

        <!-- /Stats Red Side -->

        <!-- Champion Tile Red Side -->

        <Image Grid.Column="5"
               Margin="12, 0, 0, 0"
               VerticalAlignment="Center"
               AllowDrop="{Binding ElementName=This, Path=CanSwapPlayers}"
               DataContext="{Binding ElementName=This, Path=PlayerRedSide}"
               Source="{Binding Path=Champion.SmallTileImage}"
               MouseLeftButtonDown="ChampionTileRedSide_OnMouseLeftButtonDown"
               DragEnter="ChampionTileRedSide_OnDragOver"
               DragOver="ChampionTileRedSide_OnDragOver"
               Drop="ChampionTileRedSide_OnDrop" />

        <!-- /Champion Tile Red Side -->

        <!-- Role Icon Red Side -->

        <local:LoLPositionView Grid.Column="6"
                               Margin="12, 0, 0, 0"
                               MaxWidth="{DynamicResource {x:Static theme:FlatThemeKeys.FontSizeRegular}}"
                               MaxHeight="{DynamicResource {x:Static theme:FlatThemeKeys.FontSizeRegular}}"
                               Position="{Binding ElementName=This, Path=Position}" />


        <!-- /Role Icon Red Side -->

    </Grid>
</UserControl>