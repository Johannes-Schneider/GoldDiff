<Window x:Class="GoldDiff.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:GoldDiff.View"
        xmlns:properties="clr-namespace:GoldDiff.Properties"
        xmlns:controlElement="clr-namespace:GoldDiff.View.ControlElement"
        xmlns:command="clr-namespace:GoldDiff.View.Command"
        xmlns:data="clr-namespace:GoldDiff.Data"
        mc:Ignorable="d"

        x:Name="This"

        Style="{DynamicResource {x:Static local:GoldDiffResourceKey.DefaultWindowStyle}}"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"

        Title="{x:Static properties:MainWindowResources.Title}"
        Height="600"
        Width="800">

    <Window.DataContext>
        <Binding ElementName="This" Path="PrivateModel" />
    </Window.DataContext>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controlElement:WindowControlBar Grid.Column="0"
                                         Grid.Row="0"
                                         CanClose="True"
                                         CanMinimize="True"
                                         CanMaximize="False" />

        <TextBlock Grid.Column="0"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="30, 30, 30, 18"
                   FontSize="{DynamicResource {x:Static local:GoldDiffResourceKey.FontSizeTitle}}"
                   Text="{x:Static properties:MainWindowResources.Title}" />

        <controlElement:IconControl Grid.Column="0"
                                    Grid.Row="2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="196"
                                    Height="196"
                                    Style="{DynamicResource {x:Static local:GoldDiffResourceKey.DefaultIconControlStyle}}"
                                    Source="{DynamicResource {x:Static local:GoldDiffResourceKey.IconCoins}}" />

        <StackPanel Grid.Column="0"
                    Grid.Row="3"
                    Margin="0, 18, 0, 0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <TextBlock VerticalAlignment="Center"
                       Text="{x:Static data:ApplicationConstants.Version}" />

            <controlElement:IconButton Margin="12, 0, 0, 0"
                                       Style="{DynamicResource {x:Static local:GoldDiffResourceKey.DefaultIconButtonStyle}}"
                                       Icon="{DynamicResource {x:Static local:GoldDiffResourceKey.IconCopy}}"
                                       Command="{x:Static command:Commands.Copy}"
                                       CommandParameter="{x:Static data:ApplicationConstants.Version}" />
            
            <controlElement:IconButton Margin="12, 0, 0, 0"
                                       Style="{DynamicResource {x:Static local:GoldDiffResourceKey.DefaultIconButtonStyle}}"
                                       Icon="{DynamicResource {x:Static local:GoldDiffResourceKey.IconGitHub}}"
                                       Command="{x:Static command:Commands.OpenUrl}"
                                       CommandParameter="{x:Static data:ApplicationConstants.RepositoryUrl}" />

        </StackPanel>
    </Grid>
</Window>